{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug example_lidar_udp",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/example_lidar_udp",
           //"MIMode": "lldb", // Use LLDB as the debugger
           // "miDebuggerPath": "/usr/bin/lldb", // Path to the LLDB binary
            "preLaunchTask": "build" 
            
        }
    ]
}