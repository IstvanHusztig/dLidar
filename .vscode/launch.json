{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug dLidar",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/dLidar",
            //"MIMode": "lldb", // Use LLDB as the debugger
            // "miDebuggerPath": "/usr/bin/lldb", // Path to the LLDB binary
            "preLaunchTask": "build"
        }
    ]
}